# make/cbuff

CC = cc
CFLAGS = -Wall -Wextra -std=c99 -I$(HOME)/git/ds/inc -g

SRC = $(HOME)/git/ds/src/bit_arr.c
TEST = $(HOME)/git/ds/test/bit_arr_test.c
OUT = $(HOME)/git/ds/bin/debug/bit_arr_test

all: $(OUT)

$(OUT): $(SRC) $(TEST)
	$(CC) $(CFLAGS) $(SRC) $(TEST) -o $(OUT)

run: all
	$(OUT)

clean:
	rm -f $(OUT)

